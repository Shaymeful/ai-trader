# Core dependencies
pydantic>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Windows single-instance guard (Windows only)
pywin32>=311; sys_platform == 'win32'

# Testing
pytest>=7.4.0

# Optional: Alpaca integration (not required for mock mode)
alpaca-py>=0.20.0
